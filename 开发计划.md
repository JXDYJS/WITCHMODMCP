# WitchModMCP 开发计划

## 架构总览

```
┌──────────────────────────────────────┐     HTTP POST /mcp      ┌──────────────┐
│         游戏进程 (Unity)              │ ◄─────────────────────► │  外部 AI     │
│                                      │   JSON-RPC 2.0          │  (Claude /   │
│  ┌──────────────────────────────┐    │   localhost:3100         │   Copilot)  │
│  │     Unity 主线程              │    │                          └──────────────┘
│  │  ├─ 游戏 Update              │    │
│  │  ├─ Harmony Hook 回调        │    │
│  │  └─ Dispatcher 队列 ← 消费    │    │
│  └──────────┬───────────────────┘    │
│             │ RunOnMainThread         │
│  ┌──────────▼───────────────────┐    │
│  │   后台线程 (HttpListener)     │    │
│  │  ├─ 监听 localhost:MCPPort   │    │
│  │  ├─ 接收 JSON-RPC            │    │
│  │  ├─ 反序列化 → 路由到工具     │    │
│  │  ├─ 通过 Dispatcher 切主线程  │    │
│  │  └─ 写回 JSON-RPC 响应       │    │
│  └──────────────────────────────┘    │
└──────────────────────────────────────┘
```

---

## 实现步骤（共 5 步）

### 第 1 步：GameDispatcher + DispatcherBehaviour（主线程编组）

**文件：** `Dispatcher/GameDispatcher.cs`, `Dispatcher/DispatcherBehaviour.cs`

**核心逻辑：**
- `GameDispatcher.Initialize()` → 在 `[ModInitialize]` 调用，保存 `SynchronizationContext.Current`
- `_queue` → `ConcurrentQueue<Func<Task>>` 任务队列
- `GameDispatcher.Update()` → 由 `DispatcherBehaviour.Update()` 每帧调用，逐个出队执行
- `RunOnMainThread<T>(Func<T>)` → 后台线程调用，返回 `Task<T>`

**消费队列的机制：**
- `Entry.cs` 中 `new GameObject("WitchModMCP_Dispatcher").AddComponent<DispatcherBehaviour>()`
- `DispatcherBehaviour` 继承 `MonoBehaviour`，在 `Update()` 中调用 `GameDispatcher.Update()`
- Unity 自动每帧调用 `MonoBehaviour.Update()`，无需任何 Hook
- 加 `Object.DontDestroyOnLoad(go)` 防止场景切换时销毁

**关键点：**
- `Initialize()` 必须在 Mod 入口（主线程）调用
- `DispatcherBehaviour` 定义在 Mod 的 DLL 中，Unity 运行时自动发现，与写在 `Assembly-CSharp` 中无异

---

### 第 2 步：读取 MCPPort 配置

**文件：** `Entry.cs`

ModConfig.json 新增了 `"MCPPort": 3100`。游戏自带的 `ModConfig` 类不认识这个字段，所以需要在 `[ModInitialize]` 中自行读取 JSON：

```
DLL 路径 → Assembly.GetExecutingAssembly().Location
Mod 根目录 → 上一级目录 (Scripts/../)
ModConfig.json 路径 → Mod根目录/ModConfig.json
```

用 `Newtonsoft.Json.Linq.JObject.Parse(File.ReadAllText(path))` 提取 `MCPPort`。

---

### 第 3 步：McpServer（HTTP 服务器）

**文件：** `MCP/McpServer.cs`

**核心逻辑：**
- 用 `HttpListener` 监听 `http://localhost:{port}/`
- 只处理 `POST /mcp` 路径
- 读取请求体 JSON → 传给 `McpRouter.HandleRequest()`
- 写回 JSON 响应

**关键点：**
- `HttpListener` 是 .NET Framework 自带类，无需 NuGet
- 启动时需调用 `_listener.Prefixes.Add(...)` 注册 URL
- `ListenLoop()` 用 `await _listener.GetContextAsync()` 异步等待
- 每个请求并行处理（`_ = HandleRequest(ctx)`）
- 后台线程启动，不阻塞主线程

**注意：**
- 需要在 `Entry.cs` 启动时调用 `new McpServer().Start(port)`
- 游戏退出时调用 `Dispose()` 停止监听
- 注意 `HttpListener` 在部分 Windows 版本可能需要管理员权限——但监听 localhost 不需要

---

### 第 4 步：McpRouter（JSON-RPC 路由）

**文件：** `MCP/McpRouter.cs`, `MCP/Models/*`

**核心逻辑：**
- 解析 JSON-RPC 2.0 请求 (`method`, `id`, `params`)
- 根据 method 分发：
  | method | 说明 |
  |--------|------|
  | `initialize` | 返回协议版本 + 服务器能力信息 |
  | `notifications/initialized` | 客户端通知，无需响应 |
  | `tools/list` | 返回注册的工具列表 |
  | `tools/call` | 调用指定工具 |
- 返回 JSON-RPC 响应或错误

**工具管理：**
- 用 `Dictionary<string, Func<JToken, Task<JToken>>>` 注册工具
- `RegisterTool(name, handler)` 添加工具
- `HandleToolCall(params)` 查找并执行工具

**错误码：**
| code | 含义 |
|------|------|
| -32700 | 解析错误 (Parse error) |
| -32600 | 无效请求 (Invalid Request) |
| -32601 | 方法不存在 (Method not found) |
| -32602 | 无效参数 (Invalid params) |
| -32603 | 内部错误 (Internal error) |

**参考：** 主 prompt 中的 McpRouter 代码段

---

### 第 5 步：工具注册与实现

**文件：** `Tools/IMcpTool.cs`, `Tools/PlayerTools.cs`, `Tools/GameStateTools.cs`, `Tools/LogTools.cs`

**首批工具：**

| 工具名 | 参数 | 返回 | 实现方式 |
|--------|------|------|----------|
| `get_player_health` | 无 | `{hp, maxHp}` | `RoleTable.Instance.CurHp / MaxHp` |
| `get_player_money` | 无 | `{money}` | `(int)RoleTable.Instance.Money` |
| `get_game_state` | 无 | `{scene, inFight, ...}` | `UIManager` + `FightManager.Instance` |
| `get_log` | `{count: 50}` | `{logs: []}` | `LogBuffer.GetRecent()` |
| `echo` | `{text}` | `{text}` | 测试用，直接返回 |

**注意：**
- 所有涉及 Unity API 的操作必须通过 `GameDispatcher.RunOnMainThread()` 执行
- 纯 C# 操作（如读 LogBuffer）可以直接在后台线程执行
- 每个工具方法用 `try-catch` 包裹

**参考：** 
- API 文档：`RoleTable` (玩家数据)、`FightManager` (战斗)、`GameConfigManager` (配置表)
- `Utils/ReflectionHelper.cs` 可用于访问私有字段

---

## 补充说明

### ModConfig.json 自定义字段
```json
{
  "ModName": "WitchModMCP",
  "MCPPort": 3100
}
```
游戏会忽略未知字段，Mod 启动时自行读取。

### 线程安全规则
| 操作 | 线程 |
|------|------|
| Unity API (GameObject, Transform, GetComponent) | 必须主线程 |
| 读写纯 C# 数据 (LogBuffer, Dictionary) | 任意线程 |
| Harmony Patch 回调本身 | 主线程 |
| HttpListener 接收请求 | 后台线程 |

### 可用游戏 API 速查
| API | 位置 | 用途 |
|-----|------|------|
| `RoleTable.Instance` | 全局单例 | 玩家 HP、金钱、牌组、圣物 |
| `FightManager.Instance` | 战斗时 | 战斗状态、回合控制 |
| `UIManager.Instance` | 全局单例 | 打开/关闭 UI |
| `GameConfigManager.Instance` | 全局单例 | 读取所有配置表 |
| `EventCenter.Instance` | 全局单例 | 全局事件监听 |
| `FightCardManager.Instance` | 战斗时 | 手牌、抽牌堆 |
| `TempDataManager.Instance` | 全局单例 | 临时数据 |
| `Commands.Log(tag, msg)` | 全局工具 | 向游戏控制台输出日志 |

### 依赖清单
项目当前依赖（都在游戏 `Managed/` 目录下）：
- `Witch.dll` — 游戏主程序集
- `Witch.Core.dll` — 游戏核心框架
- `UnityEngine.CoreModule.dll` — Unity 核心
- `Newtonsoft.Json.dll` — JSON 序列化
